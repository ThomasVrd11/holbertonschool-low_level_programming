#include "main.h"
#include <stdio.h>
/**
* _strlen- retourne a la taille d'un string
* @s: le charcatere a getstring
* Return: retourne le compte final
*/
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);

}
